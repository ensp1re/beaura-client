import SettingsPage from '@/app/components/root/Settings'
import React, { FC, ReactElement } from 'react'

const Settings: FC = (): ReactElement => {
    return (
        <SettingsPage />
    )
}

export default Settings