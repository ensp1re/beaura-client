import PreviewTransformationComponent from '@/app/components/root/Preview'
import React, { FC, ReactElement } from 'react'

const PreviewTransformation: FC = (): ReactElement => {
  return (
    <PreviewTransformationComponent />
  )
}

export default PreviewTransformation