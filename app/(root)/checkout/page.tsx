import CheckoutPage from '@/app/components/root/Checkout'
import React, { FC, ReactElement } from 'react'

const Checkout: FC = (): ReactElement => {
  return (
    <CheckoutPage />
  )
}

export default Checkout