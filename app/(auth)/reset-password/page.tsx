import ResetComponent from '@/app/components/auth/ResetComponent'
import React, { FC, ReactElement } from 'react'

const ResetPassword: FC = (): ReactElement => {
    return (
        <ResetComponent />
    )
}

export default ResetPassword