import ForgotComponents from '@/app/components/auth/ForgotComponents'
import React, { FC, ReactElement } from 'react'

const ForgotPassword: FC = (): ReactElement => {
    return (
        <ForgotComponents />
    )
}

export default ForgotPassword