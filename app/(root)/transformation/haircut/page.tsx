import ImageTransformer from '@/app/components/root/CreateImage'
import React, { FC, ReactElement } from 'react'

const Transformation: FC = (): ReactElement => {
    return (
            <ImageTransformer />
    )
}

export default Transformation